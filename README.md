# tarun

````javascript
var object = {

add: function(x, y) {
    return x+y;
  },
sub: function(x, y) {
    return x-y;
  },
multi: function(x, y) {
    return x*y;
  },
div: function(x, y) {
    return x/y;
  }
}
````
